<?xml version="1.0" encoding="utf-8"?>
<!--
  Step status icon — error state (filled red circle with X).
  Used in BridgeSetupActivity step rows when a step check fails.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">

    <!-- Filled circle -->
    <path
        android:fillColor="#EF4444"
        android:pathData="M12,2 A10,10 0 1,1 11.999,2 Z" />

    <!-- X mark — first stroke -->
    <path
        android:fillColor="@android:color/transparent"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="2"
        android:strokeLineCap="round"
        android:pathData="M8.5,8.5 L15.5,15.5" />

    <!-- X mark — second stroke -->
    <path
        android:fillColor="@android:color/transparent"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="2"
        android:strokeLineCap="round"
        android:pathData="M15.5,8.5 L8.5,15.5" />

</vector>
